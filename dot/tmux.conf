#Solazired configuration
set -g default-terminal "screen-256color"

# enable copy-paste http://goo.gl/DN82E
set -g default-command "reattach-to-user-namespace -l zsh"

# source $HOME/solarized/tmux/tmuxcolors-256.conf

source /Users/brunomalvestuto/Library/Python/2.7/lib/python/site-packages/powerline/bindings/tmux/powerline.conf

# source $HOME/.vim/bundle/powerline/powerline/bindings/tmux/powerline.conf

# act like vim
setw -g mode-keys vi
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind-key -r C-h select-window -t :-
bind-key -r C-l select-window -t :+
